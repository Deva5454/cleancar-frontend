/**
 * SyncInitializer — no-op component (Supabase removed)
 * 
 * Renders nothing. Kept so AppProvider import compiles without changes.
 */

export function SyncInitializer(): null {
  return null;
}
